export enum ActionState {
  HIDDEN,
  ADD_ITEM,
  SHOPPING_LIST,
  ITEM_INFO,
}

export enum UpdateQtyMode {
  INCRESE = 1,
  DECREASE = -1,
}

export enum ShoppingMode {
  EDIT,
  SAVED,
}

export enum ShoppingStatus {
  COMPLETED = 0,

  CANCELED = 1,
}
